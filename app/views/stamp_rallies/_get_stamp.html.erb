<% if user_signed_in? %>
  <% if current_user.participate?(stamp_rally) %>
    <button onclick="getLocationAndDisplayStamps(<%= stamp_rally.id %>)", class="btn btn-info">スタンプゲット</button>
  <% end %>
  <script>
      var alreadyAcquiredStamps = <%= current_user.acquired_stamp_ids.to_json.html_safe %>;
  </script>
<% end %>
